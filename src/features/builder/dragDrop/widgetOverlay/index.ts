// src/features/builder/dragDrop/widgetOverlay/index.ts

// Export the manager
export * from '../WidgetOverlayManager';
export { default as widgetOverlayManager } from '../WidgetOverlayManager';

// Export the overlay component
export { default as WidgetDropOverlay } from '../../components/WidgetDropOverlay';

// Default export for the overlay component
export { default } from '../../components/WidgetDropOverlay';